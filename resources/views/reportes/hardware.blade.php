@extends('layouts.app')
@section('title', 'Reporte de hardware')
@section('page_title', 'Reporte de hardware')
@section('content')
<div class="mb-4 flex flex-wrap items-center gap-3"><form method="GET" class="flex-1 sm:max-w-md"><input name="q" value="{{ $q }}" placeholder="Filtrar por serial..." class="h-10 w-full rounded-xl border border-slate-200 bg-white px-4 text-sm shadow-card"></form><a href="{{ route('reportes.hardware.csv', ['q' => $q]) }}" data-no-loader class="rounded-xl bg-brand-600 px-4 py-2.5 text-sm font-bold text-white hover:bg-brand-700"><i class="bi bi-download"></i> CSV</a></div>
<div class="overflow-x-auto rounded-3xl border border-slate-200 bg-white shadow-card"><table class="w-full min-w-[800px] text-left text-sm"><thead class="border-b border-slate-100 bg-slate-50 text-xs uppercase text-slate-400"><tr><th class="px-5 py-3">Serial</th><th class="px-5 py-3">Equipo</th><th class="px-5 py-3">Tipo</th><th class="px-5 py-3">Modelo</th><th class="px-5 py-3">Usuario</th><th class="px-5 py-3">Estado</th></tr></thead><tbody class="divide-y divide-slate-50">@foreach($hardware as $equipo)<tr class="hover:bg-brand-50/40"><td class="px-5 py-3 font-mono text-xs">{{ $equipo->Hw_Serial }}</td><td class="px-5 py-3 font-semibold">{{ $equipo->Hw_Nombre }}</td><td class="px-5 py-3">{{ $equipo->tipo->Nombre ?? '—' }}</td><td class="px-5 py-3">{{ $equipo->modelo->Nombre ?? '—' }}</td><td class="px-5 py-3">{{ $equipo->usuarioInv->UsuarioInvNombre ?? '—' }}</td><td class="px-5 py-3">{{ $equipo->Activo ? 'Activo' : 'Inactivo' }}</td></tr>@endforeach</tbody></table></div>
@endsection
